import jwt
from datetime import datetime, timedelta, timezone

expedition = datetime.now(timezone.utc) + timedelta(hours=1)

payload = {
    "sub": '67803287a3c6dc117857019e',
    "exp": expedition,
    "username": "Hacker"
}

pwd = 'SECRET'

token = jwt.encode(payload, pwd, algorithm='HS256')

